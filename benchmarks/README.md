Vendored from [yandex/yambda](https://huggingface.co/datasets/yandex/yambda): evaluation metrics, time splits, SASRec and popularity baselines.

Used by `eval_decoder.py` and training eval hooks. Not installed as a separate package — import path is wired in the training/eval scripts.
